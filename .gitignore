# === Secrets & credenciales ===
.env
.env.*
!.env.example
*.key
*.pem
credentials*
secrets*

# === Python ===
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
*.egg-info/
venv/
.venv/
env/

# === macOS ===
.DS_Store
.AppleDouble
.LSOverride

# === Backups locales ===
*.bak
*.backup
*.backup-*
*.bak-*

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === Logs ===
logs/
*.log

# === Data sensible / regenerable ===
# macro.db is versioned for persistence across GitHub Actions runs
# (runners are ephemeral, so SQLite must be in repo to persist between days)
# data/*.db                # COMMENTED: now versioned
data/*.db.backup*           # local backups remain ignored
data/*.db.pre-*             # pre-migration backups remain ignored
data/noticias_cache.json
my_portfolio.json

# === Data regenerable adicional ===
data/web_data.json
